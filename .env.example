# MediaFlow Agent 环境变量示例
# 说明：API Key 等敏感信息建议在插件的「设置页」中配置并保存到 chrome.storage.local，
# 不要硬编码在仓库里。以下变量仅用于本地开发联调时的默认值（可选）。

# 默认模型服务 Base URL（OpenAI 兼容接口）
WXT_DEFAULT_BASE_URL=https://api.deepseek.com/v1

# 默认模型名称
WXT_DEFAULT_MODEL=deepseek-chat
